# FT8 Fox Mode — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.
> Each implementer: run `cargo fmt --all` + `cargo check --workspace --all-targets --features transmit` before committing.

**Goal:** Manual Fox-mode — run as the DXpedition: repeating CQ + work up to `fox_max_streams` Hounds concurrently (multi-streamed, de-conflicted), each sequenced to RR73 + logged. Mostly reuse (#39 answer-callers, #40 multi-TX coalescer, the shipped offset de-confliction, `start_cq_manual`); thin new = a Fox-mode toggle + a cap raise.

**Spec:** `docs/superpowers/specs/2026-06-28-fox-mode-design.md`. **Decisions:** toggle key `x` (foX; else `Shift+X`); first-come up to cap; standard messages; manual-engaged only.

**Branch:** `feat/fox-mode` off main.

---

## Task 1: `FoxConfig` + fox_mode state
**Files:** `pancetta-config/src/fox.rs` + `lib.rs`; `pancetta/src/coordinator/mod.rs`.
- [ ] `FoxConfig { max_streams: usize (default 5) }` (mirror `HoundConfig`'s `ConfigSection`/validate style; validate `1..=8` since `MAX_RETAINED_TX_STREAMS=8`). Add `#[serde(default)] pub fox` to `Config`.
- [ ] `fox_mode: Arc<AtomicBool>` (default false) + accessor on `ApplicationCoordinator` (mirror `gateway_enabled`/`tx_offset_hold_hz`).
- [ ] Tests: config defaults + validation reject >8 / 0. `cargo test -p pancetta-config`; `cargo build -p pancetta`. Commit.

## Task 2: engage/disengage Fox (CQ loop + cap raise)
**Files:** `pancetta/src/coordinator/qso.rs` (+ `message_bus.rs`).
- [ ] READ the always-answer-callers cap in `maybe_answer_caller` (~`coordinator/qso.rs:828`, the `max_concurrent_qsos` gate ~`:975`) and `start_cq_manual`/`StopCq` handling. Determine the effective concurrent-answer cap source.
- [ ] `QsoMessage::SetFoxMode { on: bool }` (bus). Handler: on `true` → set `fox_mode` atomic, start the repeating CQ (reuse the manual `StartCq`/`start_cq_manual` path), and make the answer-path use `fox_max_streams` as the cap; on `false` → `StopCq` (cancel `CallingCq`) + clear `fox_mode` + restore the normal cap.
- [ ] Make `maybe_answer_caller`'s cap read `fox_max_streams` when `fox_mode` is set (thread the value/atomic in). Each answer stays `respond_to_caller` (already de-conflicts + sets `partner_freq` + multi-streams). NO change to sequencing/logging.
- [ ] TX-policy gate the engage (refuse under RespondOnly/Disabled, like CallStation).
- [ ] Tests: engage → fox_mode set + CQ QSO created; cap is `fox_max_streams` while on, normal while off (regression). `cargo test -p pancetta`. Commit.

## Task 3: concurrent-answer multi-stream proof (engine/coord)
**Files:** `pancetta-qso` tests and/or `pancetta/tests/coord_sim.rs`.
- [ ] Prove: with Fox mode on (cap ≥2), two Hounds calling us in the same window are BOTH answered and their replies coalesce into ONE `MultiTransmitRequest` on distinct de-conflicted offsets (rig-level via coord_sim: ≥2 streams keyed in one slot, offsets ≥75 Hz apart). And the (cap+1)th caller is not answered until a slot frees. Fox-off → normal cap (regression).
- [ ] `cargo test -p pancetta --test coord_sim`. Commit.

## Task 4: TUI Fox toggle + indicator
**Files:** `pancetta-tui/src/{tui_runner.rs,...}`, `coordinator/tui_relay.rs`.
- [ ] `TuiCommand::ToggleFoxMode`; bind `x` (verify free; else `Shift+X`) → relay → `QsoMessage::SetFoxMode { on }` (TX-policy gated). Title-bar "FOX (N max)" chip when on. Callers panel + TX strip already show the queue/streams.
- [ ] `cargo build -p pancetta-tui -p pancetta`; `cargo test -p pancetta-tui`. Commit.

## Task 5: docs + review + land
- [ ] CLAUDE.md Fox bullet (reuse-heavy; toggle; cap raise; multi-stream via #40 + offset de-confliction). Note manual-only + standard-messages (SuperFox separate).
- [ ] Final code-review subagent (focus: fox_mode-off regression = today's #39 cap; CQ+answers all same parity coalesce; cap raise reversible; ≤8 streams).
- [ ] fast gate → PR → **wait CI green** → merge; sync main.

---
## Self-review
- **Regression:** fox_mode off ⇒ #39 cap + behavior unchanged.
- **Reuse:** no new modulation/messages/codec; offset de-confliction + multi-TX coalescer do the parallel streams.
- **Cap ≤ MAX_RETAINED_TX_STREAMS (8).**
