//! Integration tests for the complete audio pipeline
//!
//! TODO: Re-enable when the coordinator API is stabilized and the
//! point-to-point channel architecture is fully wired.
//! These tests need to be rewritten to match the new coordinator API
//! (ApplicationCoordinator::new takes 8 args, uses crossbeam channels, etc.)
