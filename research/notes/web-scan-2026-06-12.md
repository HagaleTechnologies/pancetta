# Web scan 2026-06-12 — inaugural deep-research sweep (Batch 96)

Source: deep-research workflow (104 agents, 5 search angles, 15 sources
fetched, 3-vote adversarial verification per claim). All sources academic
(IEEE/arXiv/EURASIP) — no GPL clean-room firewall needed; implementation
can proceed from published equations.

Ranked candidates (bank entries hb-252..hb-259, dead end hb-260):

1. **BICM-ID iterative demod-decode for NC-MFSK** (hb-252, 0.70): feed
   LDPC extrinsic LLRs back into per-symbol tone-LLR computation.
   Pancetta's pipeline IS the degenerate all-zeros case of the SOMAP
   formula (Valenti & Cheng 2005 eq. 8). Measured 0.34-1.04 dB by
   modulation order; M=8 interpolates ~0.4-0.7 dB. 12-0 verification.
   Caveat: measured on long turbo codes; 174-bit block may realize less.
2. **Exact Bessel noncoherent LLR vs dual-max** (hb-253, 0.55): ~0.6 dB
   measured gap for not knowing Es/N0; needs per-candidate noise
   estimation. Pairs with hb-252. JSAC 2024 adds Doppler-aware variants.
3. **Post-BP-failure saturation/perturbation retry** (hb-254, 0.55):
   EQML saturation lists + multi-round BP with perturbed VNs; attacks
   the BP-non-convergence population directly. Honesty note: pancetta's
   truth-adjacent BP failures sit at ~68% sign agreement (B91) — far
   from the waterfall where literature gains were measured; probe on
   near-converged failures first.
4. **BP-RNN decoder diversity + OSD** (hb-255, 0.40, plan-sized):
   within 0.2 dB of ML on CCSDS(128,64); extends pancetta's existing
   neural-OSD line; training infra needed (GPU meatspace).
5. **Robust impulsive-noise LLR** sign(y)·min(a|y|, b/|y|) (hb-256,
   0.50): closed-form, dominant HF noise is lightning static; cheap
   probe on existing storm-day corpora if any (corpus characterization
   has per-day noise stats).
6. **Ensemble decoding for short LDPC** (hb-257, 0.35): parallel
   constituent decoders + ML selection; architectural cousin of killed
   sequential ideas, but the parallel-ensemble form is untried.
7. **Trajectory-informed neural OSD ordering** (hb-258, 0.35): ~0.2 dB
   from better bit ordering at same TEP budget; bp_trajectory_capture.rs
   already exists in-tree as the data source.
8. **Per-iteration EM channel re-estimation** (hb-259, 0.40): companion
   to hb-252; within 0.6 dB of perfect-CSI without AGC assumptions.
9. **GRAND family: DEAD END** (hb-260, documented negative): designed
   for high-rate codes; FT8's rate-0.52 / 83 redundancy bits is far
   outside tractable guessing range. Do not pursue.

Next scan: cloud routine `pancetta-research-scan` runs Mondays 12:03 UTC.
