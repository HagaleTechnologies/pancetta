# Batch 97 — hb-252 BICM-ID: MECHANISM CONFIRMED (+0.38 dB) — the web-sweep pipeline pays on its first try

**Date**: 2026-06-12
**Branch**: `iter/2026-06-12-batch-97`
**Status**: MECHANISM-CONFIRMED-FP-PENDING — the first genuine sensitivity-gain mechanism of the entire recovery push, implemented from published equations within hours of the Batch 96 web sweep surfacing it.

## Results

**Synthetic kill-switch (pre-registered bar ≥0.2 dB): PASSED.**
50%-decode-rate threshold −18.73 → −19.11 dB at 2 global iterations
(+0.384 dB; +0.439 at 4) — inside the literature's predicted band for
M=8. At −19 dB the decode rate jumps 38% → 58-64%. Convergence is
fast; rescue cost applies only to CRC-failing candidates.

**Real-corpus spot (hard_200/50, hash-normalized)**: ΔTP +3 / ΔFP +21.
Real rescues happen, but rescued BP runs sometimes converge to wrong
CRC-passing codewords. The FP bar (ΔFP ≤ 2×ΔTP) failed → no default
change yet.

## Implementation

Valenti & Cheng eq. 8 max-log SOMAP, transcribed from the fetched PDF
(not memory): on CRC failure, BP extrinsic (posterior − channel) feeds
back as a-priori into per-symbol tone-LLR extraction with the p≠i
exclusion, then BP re-runs. `Ft8Config::bicm_id_iterations` default 0 =
byte-identical (tested). Primary parallel decode path only (kill-switch
scope).

## Batch 98 (graduation path)

1. Stamp BICM-ID-rescued decodes with a dedicated `decode_origin` level
   — the shipped v3 content gate and suspicion filters then price them
   (this is exactly what the origin ordinal was built for).
2. Restrict rescue to candidates with few unsatisfied parity checks
   (the near-converged population).
3. Full-corpus measure (raw_530_full + hard_1000) at the gated config.

## Counters

- Decoder: +SOMAP rescue loop behind default-0 flag, byte-identical
  default (tested); 519+ tests green
- hb-252: PROPOSED → MECHANISM-CONFIRMED-FP-PENDING in one day from
  literature discovery to measured mechanism
