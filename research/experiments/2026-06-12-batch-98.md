# Batch 98 — BICM-ID gated: SHIP-OPT-IN; syndrome gating refuted as FP discriminator

**Date**: 2026-06-12
**Branch**: `iter/2026-06-12-batch-98` (implementer agent, commit c18a46b3)
**Status**: SHIP-OPT-IN — the +0.4-0.5 dB mechanism stands (synthetic, gated: +0.500 dB), but real-corpus FP economics fail at every gating threshold; default stays 0. The batch's chief scientific product is a refutation.

## The refutation (pre-registered instrument, honest result)

Unsatisfied-parity-check distribution of rescued candidates on
hard_200/50: **wrong-CRC rescues track true rescues within ~1pp at every
threshold** (cumulative keep at T=18: 80.9% true vs 79.8% FP). The
near-converged hypothesis is dead as an FP discriminator — syndrome
weight carries no signal about whether a rescue lands on the true
codeword. It survives only as a wall-clock pruner (−24.5% futile
rescues). Gated spot frontier: T=18 → ΔTP +2/ΔFP +17; T=8 → +0/+8;
T=5 → +0/+1 — TPs and FPs shed in lockstep.

## What shipped (opt-in surface)

`bicm_id_max_unsatisfied_checks` (default 18, test-pinned),
`decode_origin = Some(7)` for rescues (clamps to max v3 lateness
penalty — documented, divisor unchanged), unconditional suspicion
scrutiny on rescues, and a zero-cost-when-off instrumentation hook
(`PANCETTA_BICM_ID_INSTRUMENT_FILE`). 527 pancetta-ft8 tests green;
default byte-identity intact (Batch 97 baseline reproduced exactly).

## Disposition

hb-252 → SHIP-OPT-IN with re-open condition: a *different* rescue
discriminator — the hb-253 exact Bessel metric or hb-259 EM channel
re-estimation could make rescue LLRs sharp enough that wrong-codeword
convergence drops; the unsatisfied-check axis is measured flat and
must not be re-tuned. Full-corpus run correctly skipped per
pre-registration (spot bar failed).

## Counters

- Ships: opt-in config surface + instrumentation; default unchanged
- Science: 1 refutation (syndrome gating), 1 mechanism preserved
  (+0.500 dB gated synthetic)
- hb-252: MECHANISM-CONFIRMED-FP-PENDING → SHIP-OPT-IN
