# Batch 98 — hb-252 BICM-ID FP control: SHIP-OPT-IN, gate premise refuted honestly

**Date**: 2026-06-12
**Branch**: `iter/2026-06-12-batch-98`
**Status**: SHIP-OPT-IN — the three FP controls shipped (inert at
default), the near-converged gate measured, the default flip declined
by the pre-registered bars.

## Results

**Instrumentation (hard_200/50, gate off)**: 299 truth-matching rescue
emissions vs 104 wrong-CRC emissions vs 10,734 failed attempts. The
unsatisfied-check distributions of true and wrong-CRC rescues are
nearly identical (cumulative keep at the chosen T=18: 80.9% vs 79.8%)
— **near-converged gating does not discriminate true from false
rescues**; it only prunes futile work (-24.5% of failed attempts).

**Gated spot frontier (hard_200/50, iters 0→2)**: T=5 → ΔTP +0/ΔFP +1;
T=8 → +0/+8; T=18 → +2/+17; ungated (B97) → +3/+21. TPs and FPs shed
in lockstep — no threshold passes ΔFP ≤ 2×ΔTP with ΔTP > 0, so the
pre-registered full-corpus graduation (raw_530_full + hard_1000) never
triggered.

**Synthetic re-check at T=18 (10 trials/pt)**: threshold shift
**+0.500 dB** (bar ≥0.2 PASSED) — the gate doesn't harm the mechanism;
synthetic true rescues are near-converged.

## Shipped (active only when an operator opts in with `bicm_id_iterations ≥ 1`)

1. `Ft8Config::bicm_id_max_unsatisfied_checks` (default 18, test-pinned)
   — seed-BP syndrome count gates the rescue.
2. `decode_origin = Some(7)` for rescued decodes — the shipped hb-103
   v3 gate prices them at max lateness penalty (7/6 clamps to 1.0;
   divisor 6 unchanged by design, documented).
3. Unconditional suspicion scrutiny for rescued decodes (primary path
   keeps its confidence-conditional gate; iters=0 byte-identity
   re-verified: 1167/309 on the spot, identical to Batch 97).

Plus `PANCETTA_BICM_ID_INSTRUMENT_FILE` research instrumentation
(OnceLock-gated, one branch when off).

## Verdict and counters

- hb-252: MECHANISM-CONFIRMED-FP-PENDING → **SHIP-OPT-IN**. Re-open
  only with a different discriminator (hb-253 exact metric, hb-259
  Es/N0), not another unsat tune — that axis is measured and flat.
- Tests: 527 passed / 0 failed / 2 ignored (`--features transmit`,
  exit 0); +2 new gate tests.
- Production default unchanged (byte-identical, double-guarded).

Full distribution table + frontier:
`research/notes/2026-06-12-batch98-bicm-id-gated.md`. Harness:
`pancetta-research/examples/batch98_bicm_id_gated.rs`.
