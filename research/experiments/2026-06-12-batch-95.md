# Batch 95 — worked-before + real S-meter: the TUI lane closes

**Date**: 2026-06-12
**Branch**: `iter/2026-06-12-batch-95` (implementer agent; controller-landed)
**Status**: SHIPPED — last two gaps from the Batch 93 TUI assessment. The TUI operational lane (Batches 93-95) is complete.

## What shipped

1. **worked_before**: TUI flags come from the coordinator's existing
   `CachedStationLookup` — the same Arc and `is_duplicate` semantics the
   autonomous priority scorer uses, so display and scorer agree by
   construction (seam-tested, including the deliberate non-stripping of
   /P suffixes to match the scorer). Band Activity + DX hunter mute
   worked stations. Relay-thread lookup; render loop never blocks.
2. **Real rig S-meter**: rigctld STRENGTH read on the existing hamlib
   poll cadence (every 2s), honest units (`db_over_s9`, hamlib
   convention, S9+NN display), '---' when stale, no fake data. Fixed a
   latent bug where the first real reading would have clobbered the
   0-1 audio-level gauge.

+7 seam tests; full workspace exit 0.

## TUI lane: CLOSED (Batches 93-95)

Live: decodes (with worked-before + calling-me), waterfall + TX cursor,
QSO banner + detail panel, autonomous panel + Shift+Q→a recovery, TX
badge, S-meter, manual call/CQ/halt/tune/PTT/emergency controls.
Remaining cosmetic-only: standalone demo binary cleanup (not the
operating path).

## Counters

- Ships: TUI/relay/hamlib wiring (+442/−17); honesty fixes (units,
  gauge clobber)
