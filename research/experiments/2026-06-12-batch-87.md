# Batch 87 — hb-248 ships: hash-normalized truth scoring; corrected headline numbers on record

**Date**: 2026-06-12
**Branch**: main (direct)
**Status**: SHIPPED-INFRA — `pancetta_research::metrics::hash_normalize_message` (+5 tests) is the mandatory scoring path for all future ft8_lib-truth probes. Corrected headlines re-derived at production defaults.

## The correction (current production defaults, ft8_lib truth)

| Corpus | Precision raw → normalized | Miss rate raw → normalized | Decodes reclassified |
|---|---|---|---:|
| raw_530_full | 0.8067 → **0.8230** (+1.6pp) | 4.61% → **2.68%** | 766 |
| hard_1000 | 0.8146 → **0.8270** (+1.2pp) | 5.48% → **4.04%** | 220 |

Every reclassified decode was simultaneously a phantom miss AND a phantom
FP under exact-text scoring (`<...NNNN>` vs `<...>` rendering of the same
hashed-callsign decode). **Resolved-hash residual: 0 on both corpora** —
the conservative rule (normalize only `<...digits>` tokens, leave
`<RESOLVEDCALL>` alone) captures the entire artifact; pancetta does not
resolve hashes that ft8_lib leaves unresolved on these corpora.

## Scope and non-scope

- The helper + this probe ship. The 51 historical probes are NOT
  retrofitted: every prior ship decision compared configs under
  IDENTICAL scoring, so all deltas and verdicts stand. Their absolute
  precision/miss numbers are understated by roughly the table above.
- Forward rule (recorded in the bank): any new probe scoring against
  ft8_lib truth must intersect via `hash_normalize_message` on both
  sides, and report the raw numbers too if comparing against a
  pre-Batch-87 measurement.

## Counters

- Ships: 1 infra (scoring helper, 5 tests)
- New example: `batch87_hash_normalized_headlines.rs`
- Bank: hb-248 → SHIPPED-INFRA
- Origin: spawned and shipped same-day from the Batch 86 kill-switch
  autopsy — the second time this session a "failed" experiment's
  diagnostics outvalued its target (Batch 80 → hb-247 being the first).
