# Batch 104 — 2026-06-14 capture day: first multi-band day, sentinel clean (0.8451 / 3.84%)

**Date**: 2026-06-14
**Branch**: main (heartbeat per-day pipeline)
**Status**: COMPLETE — third consecutive fresh-capture day. First day captured by the
band-stamping recorder, and the first with real multi-band diversity. Decoder generalizes
clean; precision marginally *above* the historical band (favorable).

## Day characterization (2026-06-14, overnight session)

- 221 today slots labeled (ft8_lib truth); **219 non-empty** (1 empty, 1 unlabeled/truncated).
- **3,409 truth decodes, 15.6/slot** — healthy overnight density (vs 18.7/slot on the dense 6/13 day).
- **Three bands, band-stamped** (first time): 20m ×126, 15m ×77, 30m ×16. Directly answers the
  operator's earlier "should I spread bands out?" — this day shows the recorder now captures
  and labels band diversity end-to-end.
- Time span 00:00–01:58 UTC (≈7–9 pm Central). Full passband 121–2953 Hz.
- 664 distinct callsign-like tokens; intercontinental DX: **A92AE** (Bahrain), **D2UY** (Angola),
  **YN1J** (Nicaragua), **VK7AC** (Tasmania), **T46FCR** (Cuba), IK4LZH (Italy),
  VO1WAR (Newfoundland), plus broad NA. (T46FCR is the operator's "mostly-completed" QSO callsign.)

## Regression sentinel (production decoder vs ft8_lib truth, hash-normalized)

| Scoring | TPs | FPs | Precision | Miss rate |
|---|---:|---:|---:|---:|
| raw exact-text | 3163 | 716 | 0.8154 | 7.22% |
| hash-normalized | 3278 | 601 | **0.8451** | **3.84%** |

Resolved-hash residual mismatches: 0.

Established band: raw_530 0.823/2.68%, hard_1000 0.827/4.04%, 6/13 day 0.8238/2.87%, 6/12 day
0.8152/2.63%. **No regression.** Norm precision 0.8451 sits slightly *above* the 0.81–0.83
ceiling — precision improving, not drift — and norm miss 3.84% is comfortably under the 5% flag.
The favorable precision tracks the day's composition: a smaller overnight 15m/20m/30m sample has
cleaner propagation and fewer marginal-SNR signals, so fewer borderline FPs. Raw miss 7.22% is
elevated only because exact-text scoring penalizes hash-equivalent callsign variants (the reason
hb-248 hash-normalization exists); the norm row is the load-bearing metric.

## Decode path unchanged this batch

Today's two ships (DT slot-boundary alignment `bbd2b32f`; QSO state-regression `67a2eecf`) do
**not** touch the code this sentinel exercises: the DT fix lives in coordinator live-windowing +
`time_offset` reporting (the research harness decodes WAVs directly, not via the live window), and
state-regression is entirely in pancetta-qso. The decoder is byte-identical to the frozen tuning,
so this is a pure generalization measurement on unseen, newly-band-diverse data.

## Operator note

`~/.pancetta/qsos.adi` still has **0 logged records** despite live manual-QSO testing on 6/13–6/14.
Most likely the completing builds (reply emitter `7d83cd34` → today's regression/DT) were not yet
running during these captures, or no contact reached `Completed`. Worth confirming on the next
on-air session now that the full sequence + state-regression are live.

## Counters

- Truth files: +220 today (uncommitted bulk per Batch 71 convention).
- Committed: day_20260614.manifest.json (219 entries), headline-day_20260614.md note, this journal,
  and `scripts/build_day_manifest.py` (reusable per-day manifest builder — previously the day
  manifest was built ad hoc; this codifies the "slots with ≥1 ft8_lib decode" selection).
- Production: unchanged (measurement-only).
