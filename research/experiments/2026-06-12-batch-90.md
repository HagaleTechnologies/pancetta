# Batch 90 — hb-090 SHELVED-DEFINITIVE: matched filter validates as a demod, finds nothing at sub-Costas truth positions

**Date**: 2026-06-12
**Branch**: `iter/2026-06-12-batch-90`
**Status**: COMPLETE — the pre-registered hb-090 kill-switch ran at corrected coordinates and fired SHELVE in the cleanest form available: the mechanism demonstrably works on real signals and is exactly chance-level on the target population. Closes the hb-088 "what WOULD work" escape hatch. Spawns hb-250 (the side-finding).

## Stage A — coordinate-convention sweep (controller-run)

`PANCETTA_HB088_STEP_OFFSET` ∈ {0,1,2,3} on the hb-088 probe:

| offset | controls sign-agreement (mean) | sub-Costas median |
|---|---:|---:|
| 0 (historical) | 52.9% | 50.6% |
| +1 | 86.7% | 50.6% |
| **+2** | **91.4%** | 50.6% |
| +3 | 56.8% | 50.6% |

- The +2 row convention (Batch 88's `SLIDING_FRAME_LOOKBACK_STEPS`) is
  confirmed empirically by an independent probe.
- **hb-088's original run had its CONTROLS misaligned too** (52.9% ≈
  chance): its historical control-vs-target comparison was invalid as
  run. Its conclusion survives anyway — the now-valid contrast
  (91.4% controls vs 50.6% targets) is stronger evidence than the
  original ever was.
- Sub-Costas misses are coin-flip at every alignment: the wall is not
  misalignment, and not leakage — it is absence of extractable signal.

## Stage B — matched-filter demod (implementer agent, commit cd0b2b4b)

Phasor-recurrence complex correlation against the 8 tone templates per
symbol, same gray-code/LLR back-end, top-20 hard-200, offset +2,
n=558 controls / 576 sub-Costas:

| Population | max-log p50 | matched filter p50 | MF + ±240 refine p50 |
|---|---:|---:|---:|
| Controls | 94.3% | 95.4% | **96.6%** |
| Sub-Costas | 50.6% | 50.0% | 50.0% |

Controls sanity gate passed (MF ≥ max-log) → the mapping and demod are
right; mean|LLR| 11.79 vs 9.53 (+24%) with refinement. Sub-Costas:
exactly chance for all three demods; refinement best-shift distribution
is uniform noise at sub-Costas vs a coherent +120-sample bias on
controls. **Verdict per pre-registered bars (<58%): SHELVE.**

## Disposition

- hb-090 → SHELVED-DEFINITIVE ("premise doesn't exist" family: the
  truth-codeword energy simply isn't present at those coordinates for
  any linear front-end).
- hb-088 → corrective annotation (control baseline invalid as
  originally run; conclusion re-grounded).
- **hb-250 spawned (0.40)**: the matched-filter demod's measured +2.3pp
  control advantage as a second-chance demod for sync-passing
  LDPC-failures — the inverse population, where signal demonstrably
  exists. Premise probe is ~1 session on the Batch 90 scaffolding.

## Counters

- Production wires: 0
- New example: `batch90_hb090_matched_filter.rs`; hb088 probe gains the
  offset-sweep knob
- Bank: hb-090 SHELVED-DEFINITIVE, hb-088 annotated, +1 spawn (hb-250)
- The B86/B88 machinery (synth, LS fit, exact dt) enabled both stages —
  third mechanism this week measured properly because of it
