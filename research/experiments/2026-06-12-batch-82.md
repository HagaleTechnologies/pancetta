# Batch 82 — LDPC-iterations audit: uniform 2.2-2.4 FP/TP trade, no ship; recall lever documented

**Date**: 2026-06-11 → 2026-06-12
**Branch**: main (direct; measurement-only batch, no production change)
**Status**: COMPLETE — `ldpc_iterations` audited at the post-Batch-78 baseline on both corpora; the raise is a real recall-vs-precision trade at a uniform ~2.3 FPs per TP and 1.5-2.2× wall-clock. Stays at the current default under the precision-first posture; logged as the measured **recall lever**, symmetric to Batch 81's τ=0.90 **precision lever**.

## Context

Continuation of the Batch 78 knob audit at the new `max_sync_candidates=200`
baseline. Also closed two cheap follow-ups from Batch 81 (below).

Note: the struct default is `ldpc_iterations = 100`; the Fast hardware
tier preset rewrites to 200 at runtime (hb-216 S2), so a struct-default
change would mainly affect the Moderate tier and bare constructions.

## Results (probe: batch78_knob_audit.rs LDPC mode, ft8_lib truth)

| Corpus | ldpc=150 | ldpc=300 |
|---|---|---|
| raw_530_full (2066 slots) | +19 TPs / +37 FPs / 1.3× wall | **+42 TPs / +93 FPs** / 2.2× wall |
| hard_1000 | +6 TPs / +16 FPs / 1.2× wall | +17 TPs / +40 FPs / 1.8× wall |

The exchange rate is remarkably stable: 1.9-2.7 FPs per TP at every point
on both corpora. Unlike the osd/cands corrections (huge FP cuts at ~zero
TP cost), this knob sits on a genuine Pareto frontier — there is no free
direction.

## Verdict

No default change. The recovery push's posture (and the last three ships)
is precision-first; buying +42 TPs at +93 FPs reverses that at a mediocre
rate, plus up to 2.2× decode time. **Documented as the recall lever**:
when the operational priority shifts toward sensitivity (e.g., rare-DX
hunting in Phase 5), `ldpc_iterations: 300` is a measured one-line option,
exactly as `SHIP_CONSERVATIVE → 0.90` is the measured precision lever.
Both filed under OPERATOR-DECISION-PENDING in the bank.

## Cheap follow-ups closed (zero new decode compute)

1. **v3 weight w=−2 evaluated from the Batch 81 sample dumps**: +2.1pp FP
   rejection on hard_200, +0.0pp on raw, but min-TP margin collapses
   0.965→0.465. **w stays −1.0** (addendum in the Batch 81 note).
2. **NMS radius family ruled moot before compute**: `nms_enabled` defaults
   false (hb-019 nms-off graduation), so the radius knobs have no default
   behavior to audit.

## Counters

- Production wires: 0
- Probe updates: LDPC mode on `batch78_knob_audit.rs`
- Bank: hb-103/hb-247 entries gain the symmetric-levers note
- Knob-audit arc status: osd_depth (B72, shipped), max_sync_candidates +
  parity + min_sync_score (B78, shipped + two dead), ldpc_iterations
  (B82, frontier-documented). Remaining queued: AP thresholds.
