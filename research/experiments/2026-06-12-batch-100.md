# Batch 100 — hb-259: NO-RESCUE-FIX; the BICM-ID family closes as opt-in +0.5 dB with the CRC floor named

**Date**: 2026-06-12
**Branch**: `iter/2026-06-12-batch-100`
**Status**: COMPLETE — the web sweep's top family (hb-252/253/259) is fully resolved in one day: two real sensitivity mechanisms (additive, +0.506 dB composed, synthetic-verified, shipped opt-in), three FP discriminators measured dead, root cause named.

## Result

EM per-iteration channel re-estimation inside the rescue loop changes
nothing: spot ΔTP +1 / ΔFP +29 (vs +1/+27 EM-off; Batch 99 reproduced
exactly as the integrity check). Synthetic: no regression.

**Attribution (pre-registered)**: the rescue-FP problem is the
**CRC-collision floor**. Each extra BP attempt on a noise candidate
re-rolls CRC-14's ~1/16k dice; syndrome weight (B98), metric sharpness
(B99), and channel estimation (B100) all measured flat against it.
Corpus-side graduation for the family is closed — honestly and
completely.

## Where the family lands

- `llr_metric = Bessel` and `bicm_id_iterations = 2` (+ EM flag): real,
  additive, opt-in. +0.506 dB composed on FT8's actual block.
- Remaining validation: Phase 5 **on-air A/B** (meatspace ledger) — or
  a future corpus of marginal (0-0.5 dB-below-threshold) signals, noted
  for the corpus-expansion lane.
- The curated corpora structurally cannot reward threshold improvements
  (B91 bimodal-miss); this family is the cleanest demonstration of that.

## Counters

- Ships: opt-in EM flag (default-false, byte-identical, tests green)
- hb-259: MEASURED-NO-RESCUE-FIX; hb-252/253 final = SHIP-OPT-IN
- Web-sweep family scoreboard, day 1: 4 probes, 2 mechanisms confirmed,
  3 discriminator refutations, 1 root cause named, 1 dead-end banked
