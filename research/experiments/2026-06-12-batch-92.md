# Batch 92 — Costas half-loop: redundant for scoring, load-bearing for recall; do-not-ship (hb-251)

**Date**: 2026-06-12
**Branch**: `iter/2026-06-12-batch-92` (implementer agent, commit bbe2185b; controller-reviewed)
**Status**: COMPLETE — Batch 88's last residual closed with a measured, mechanism-explained do-not-ship. The flag stays in-tree default-OFF as executable documentation.

## Summary

The half-loop in `compute_costas_score_groups` is provably score-redundant
at TIME_OSR=2 (executable plateau-identity assertion in-tree), but removing
it costs **−64 TPs / 200 slots and −635 TPs / 2066 slots** with the dt
distribution byte-identical. Mechanism: with NMS off (default), the plateau
emits two candidates 960 samples apart per strong signal; the decode path's
fine timing search spans only ±720 samples, so the candidate PAIR jointly
covers the true alignment — the half loop is a free adjacent-alignment
retry for extraction. The Batch 88 −960 candidate-level skew is real but
absorbed downstream; reported dt never sees it.

## Verdict

Pre-registered ship test failed on ΔTP → `costas_half_loop_disabled` stays
default-OFF (kept, not reverted: the analysis was verified correct and the
flag guards any future TIME_OSR change). hb-251 SHELVED-MEASURED.

Side finding for the lever table: flag-ON is **27% faster at full scale**
(328 vs 450 s) at −1.6% TPs — a candidate Slow-tier trade if decode-budget
pressure ever returns; noted in the bank entry.

## Counters

- Production wires: 1 default-OFF flag + 1 unit test (zero-diff at default)
- Examples: batch92 A/B harness; dt-audit gains --half-loop-off/--only-a
- Closes: Batch 88 residual #1; bank +1 (hb-251 SHELVED-MEASURED)
