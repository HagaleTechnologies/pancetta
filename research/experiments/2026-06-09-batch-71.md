# Batch 71 — ft8_lib truth labels for all 25k recordings

**Date**: 2026-06-09 → 2026-06-10
**Branch**: `iter/2026-06-09-batch-71`
**Status**: INFRASTRUCTURE BATCH — generates comprehensive ft8_lib truth coverage of all 25,106 raw recordings; durable artifact (not committed; reproducible in ~3 min)

## Headline

Single tool: `batch71_ft8lib_truth_all.rs` walks any directory of WAVs and writes `<sha>.ft8lib.json` for each. Idempotent (skips existing files).

Ran on `~/.pancetta/recordings/`:
- **25,106 WAVs scanned**
- 21,752 new truth files written
- 3,353 existing files skipped (already labeled by Batches 66-70)
- **14,951 total ft8_lib decodes** across all 25k slots (0.6 avg/slot)
- 193 seconds wall-clock

The 0.6 avg/slot reflects what we already learned from Batch 65: most days are dead-band (~1 decode/slot). The 5/30 day (25/slot) is an outlier.

## Why not commit the 26k truth files?

105 MB total. They reference operator-machine-local WAV paths (`~/.pancetta/recordings/...`) which aren't useful to other developers without the underlying WAVs.

The truth labels stay on disk; the script regenerates any subset in seconds. The committed artifact is the script + the per-corpus truth files for already-curated manifests.

## Strategic value

This batch closes a research-infrastructure gap. Future shelved-mechanism investigations can:

1. Build a custom curated manifest from any subset of the 25k slots.
2. Reference the matching `<sha>.ft8lib.json` files for neutral truth.
3. Run the standard measurement probe.

The Batch 66-68 pipeline is now fully unblocked. No more "we need truth labels for this corpus before we can measure."

## Counters

- Production wires: 0 (research infrastructure)
- New examples: 1 (`batch71_ft8lib_truth_all.rs`)
- New truth files (on disk, not committed): 21,752
- Workspace tests: all green
- Cumulative recovery (Batches 33-71): +369 TPs reported (unchanged — infrastructure batch)

## Recommended Batch 72

The autonomous research loop is now at a clean infrastructure-complete state. Suggested next directions:

1. **Per-day characterization sweep** of all 41 recording days using the existing framework. Outputs the corpus-comparison table for the operator to see at-a-glance.
2. **Documentation pass** — CLAUDE.md update + a corpus-framework guide for future sessions.
3. **Operational improvements** outside the research arc.
4. **Pause for user direction** — the user explicitly asked us to "reconsider what we have" in Batch 65. We've now used what we have. The next phase is the user's call.

## Artifacts

- `pancetta-research/examples/batch71_ft8lib_truth_all.rs`
- 21,752 new on-disk `research/baselines/ft8/<sha>.ft8lib.json` files (uncommitted)
- This journal
