# Batch 91 — hb-250 SHELVED; the fixed-position extraction family is now closed at every level

**Date**: 2026-06-12
**Branch**: `iter/2026-06-12-batch-91`
**Status**: COMPLETE — pre-registered premise probe shelved hb-250 on both grounds at once (population too small AND below both quality bars). With it, the entire "mine deeper at known positions" mechanism family is closed with consistent evidence.

## Results (40 WAVs = top-20 hard-200 + 20 raw kill-switch slots)

- 2,360 deduped sync-passing LDPC-failures; only **35 (1.5%) truth-adjacent**
  — 97.7% of failed candidates point at nothing real production missed
  (force-decoding this population is FP-dominated; consistent with
  hb-088's FP-rate analysis).
- Demod comparison at the 27 evaluable truth-adjacent candidates:

| Demod | p50 sign-agreement |
|---|---:|
| spectrogram max-log | 67.8% |
| matched filter | 67.2% |
| matched filter + ±240 refine | 67.8% |

Controls reproduce Batch 90 exactly (95.4%/96.6%) — method validated.
Bars were ≥85%, or ≥75% with +8 over max-log: missed by a wide margin.

## The closed family (the week's strongest structural conclusion)

| Population | best linear demod result | meaning |
|---|---|---|
| Decoded positions | 95-97% agreement | decoder already harvests these |
| Sync-pass, LDPC-fail, truth-adjacent | **~68% plateau, BOTH demods** | corruption is in the signal (collisions/interference), not the front-end |
| Sub-Costas truth positions | exactly 50% | nothing extractable exists |

Recall on these corpora is signal-limited for any linear single-signal
processing. Together with hb-104 (no co-channel miss population after
hash normalization), this closes hb-088 / hb-090 / hb-104 / hb-250 with
one coherent account. What could still move recall lives elsewhere:
multi-receiver diversity (hb-115), AP/context priors (hb-237,
meatspace-gated), and operating-point levers (documented in bank).

## Infrastructure that remains

- `Ft8Decoder::decode_window_with_candidate_dump` (`#[doc(hidden)]`,
  additive, zero-cost when unused; dump coords share the production
  `candidate_offset_samples` convention) — the failed-candidate
  instrument future diagnostics were missing. 519 tests green.

## Counters

- Production wires: 1 additive instrument (no behavior change)
- hb-250: SHELVED (spawned Batch 90, resolved Batch 91)
- New example: batch91 premise probe; note in research/notes/
