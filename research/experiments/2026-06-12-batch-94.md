# Batch 94 — TUI QSO-detail panel live; dead message paths removed

**Date**: 2026-06-12
**Branch**: `iter/2026-06-12-batch-94` (implementer agent, commit 47d83e63)
**Status**: SHIPPED — the last substantial dead TUI panel is live; the dead plumbing that caused the original confusion is gone.

## What shipped

- `QsoMessage::QsoStateUpdate` / `TuiMessage::QsoStateUpdate` were
  **declared-but-never-constructed** dead code (plus two never-called
  TUI updaters) — all four paths removed.
- Live path: the existing `ActiveQsosSnapshot` (already pushed on every
  QSO state transition) enriched via pure `snapshot_item_from_progress()`
  — state phase, last TX/RX message + time-ago, measured RX SNR,
  reports sent/received, exchange count. Zero behavioral change to the
  QSO engine.
- Panel renders state + reports + real last messages per direction +
  SNR gauges; multi-QSO table shows State; an Hz-rendered-as-MHz
  display bug fixed in passing.
- 12 new seam tests; full workspace EXIT 0 (verified). CLAUDE.md row
  updated to "QSO-detail panel live".

## TUI lane status after Batches 93-94

Operator-facing readiness for Phase 5: live decodes, waterfall, QSO
banner + detail, autonomous panel + a-key recovery after Shift+Q, TX
badge, manual call/CQ/halt controls. Remaining (optional): S-meter,
`worked_before` logbook check, standalone demo binary cleanup.

## Counters

- Ships: TUI/coordinator wiring; −4 dead code paths; +12 tests
