# Batch 52 — hb-237 Session 3 + auto-passband + npre2 follow-up

**Date**: 2026-06-09
**Branch**: `iter/2026-06-09-batch-52`
**Status**: SHIPPED — 2 new mechanisms (default-OFF) + npre2 follow-up measurement

## Headline

Smaller batch focused on cheap unlocks: hb-237 Session 3 coordinator invocation (closes the hb-237 line) and WSJT-X Improved auto-passband (fresh new mechanism). Plus a follow-up measurement of npre2 at the correct OSD depth, confirming Batch 51's "+0 was structural inertness" finding.

**Cumulative recovery (Batches 33-52)**: +365 TPs (unchanged — Batch 52 mechanisms shipped opt-in; all default-OFF).

This batch is the final landing point of the autonomous run — user requested spin-down to restart with new model.

## What shipped

| Commit | Mechanism | LOC | Default | Tests |
|---|---|---|---|---|
| `3bcf8af` | WSJT-X Improved auto-passband (closed-form noise-floor-driven) | +567/-6 | OFF | +7 |
| `5a9f9d0` | hb-237 cross-sequence A7 coordinator invocation (Session 3) | +369/-27 | OFF | +5 |

**Total**: ~898 LOC, +12 tests, 362 → 369 pancetta-ft8 lib tests + 81 → 86 pancetta tests.

## Process

| Agent | Mechanism | Worktree commit | Cherry-pick conflicts |
|---|---|---|---|
| A | hb-237 Session 3 | `d5747d0` | Auto-merge clean (touches only coordinator/ft8.rs) |
| B | Auto-passband | `16bc1d6` | Auto-merge clean (touches only decoder.rs in new region) |
| Measurement | npre2 at OSD depth 3 | — | Probe artifact files only |

**No test-module conflicts**. Smaller batch + non-overlapping files = clean cherry-picks.

**Recurring cwd-flip hazard**: hit once again when the second worktree's completion notification arrived. Caught immediately; `cd ~/Code/pancetta` restored the main checkout and the second cherry-pick proceeded cleanly. Worth a memory note — this has happened in every multi-worktree batch since Batch 49.

## Measurement: npre2 at OSD depth 3

Probe: `pancetta-research/examples/batch52_npre2_depth3.rs`

| Config | TPs | Δ | Wall |
|---|---:|---:|---:|
| Default depth=1 baseline (Batch 51 ref) | 5301 | (reference) | — |
| OSD depth=3 + npre2 OFF | **5279** | **-22 vs ref** | 626s |
| OSD depth=3 + npre2 ON | **5285** | -16 vs ref / **+6 vs depth=3 baseline** | 560s |

**Interpretation**:

1. **Switching OSD depth 1→3 costs 22 TPs** on hard-200. Pancetta is tuned for shallow OSD; depth 3 surfaces more FPs than TPs at the corpus's SNR distribution.
2. **npre2 is mechanically correct** — it adds 6 TPs to the depth=3 baseline as the mechanism intends. But the regime where it helps is itself a regression vs pancetta's current production stack.
3. **npre2 stays default-OFF.** Pancetta's depth=1 OSD baseline is the right operating point on hard-200; npre2 has no opportunity to fire there.

This is a clean characterization: the mechanism is faithfully implemented but the corpus + pancetta-stack interaction makes it inapplicable to current production. Same family as Batch 49's hb-242 / wide-lag and Batch 51's three-stage cascade findings.

## Strategic implications

1. **The `hb-237` line is now structurally complete** through 3 sessions (cache + coordinator population + decoder consumer + coordinator invocation). End-to-end test in Session 3 demonstrated synthesized `W1AW K1ABC 73` recovery with seeded cache. Hard-200 measurement of the live end-to-end path is deferred to Batch 53.
2. **Auto-passband is novel for pancetta.** No peer FT8 decoder ships a closed-form auto-passband at this level (WSJT-X Improved has it; mainline WSJT-X uses the fixed default; ft8mon/JTDX inherit mainline). Measurement of `auto_passband_enabled = true` on hard-200 deferred to Batch 53.
3. **Pattern continues**: small-batch / focused-target with worktree discipline gives ~zero merge tax. 2 agents in parallel + 1 measurement = 1 hour wall-clock to ship 2 mechanisms.

## Recommended Batch 53 (next session)

1. **Hard-200 measurement of Batch 52 mechanisms**: `cross_sequence_a7_enabled = true` end-to-end (Session 3 path) + `auto_passband_enabled = true` baseline measurement. Should be a single probe with 3 configs (baseline / cross-seq ON / auto-passband ON / both ON). ~30 min wall-clock.
2. **WSJT-X Improved FDR (per-message-type confidence gate)**: spec available (`spec-wsjtx-improved-fdr.md`). Convergent with pancetta's hb-103 + is_plausible but worth a clean-room implementation comparison.
3. **JS8Call-Improved LLR whitening default-ON consideration**: Batch 50 measured +2 TPs / +2.7% precision. Run a `hard_1000` measurement (if corpus exists) to validate the lift holds at scale before flipping default.
4. **Build the drift-heavy corpus** for per-candidate freq tracker (Batch 50). Mobile / aurora / chirpy-rig WAVs.

## Counters

- Production wires: 2 mechanisms shipped (default-OFF)
- Tests added: +12
- pancetta-ft8 lib tests: 362 → 369
- pancetta lib tests: 81 → 86
- Workspace tests: all green
- scripts/check.sh --fast: passes
- Cumulative recovery (Batches 33-52): +365 TPs (unchanged)
- npre2: SHELVED-AT-CURRENT-OSD-DEPTH (mechanism faithful but regime where it helps is itself a regression vs production default)

## Artifacts

- Commits `3bcf8af`, `5a9f9d0` on `iter/2026-06-09-batch-52`
- `pancetta-research/examples/batch52_npre2_depth3.rs`
- `research/notes/2026-06-09-batch52-npre2-measurement.md`
- This journal
